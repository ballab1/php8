#!/bin/bash

function php.fixupDirectory()
{
    local -r dir="${1:?'Input parameter "dir" must be defined'}"
    
    [ -d "${dir}" ] || mkdir -p "${dir}"
    find "$dir" -type d -exec chmod 777 '{}' \;
    find "$dir" -type f -exec chmod 666 '{}' \;
}
export -f php.fixupDirectory